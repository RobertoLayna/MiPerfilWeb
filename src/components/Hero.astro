<section class="hero">
  <div class="container hero-grid">
    <div>
      <p class="hero-eyebrow reveal" data-reveal-delay="0">Desarrollador Full Stack</p>
      <h1 class="reveal" data-reveal-delay="100">Construyo productos<br>que <span>escalan.</span></h1>
      <p class="lead reveal" data-reveal-delay="200">Roberto Robles Layna — desarrollo full stack con Vue, React, Next.js, Nuxt, NestJS, Python y Flutter. De la idea al deploy.</p>
      <div class="hero-actions reveal" data-reveal-delay="300">
        <a href="#contact" class="btn btn-primary">Conversemos</a>
        <a href="#projects" class="btn btn-secondary">Ver proyectos</a>
      </div>
    </div>
    <div class="hero-visual">
      <div class="placeholder-avatar" id="avatar">RRL</div>
    </div>
  </div>
</section>

<style>
.hero {
  padding: var(--section-y-desktop) 0;
}
.hero-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-12);
  align-items: center;
}
.hero-eyebrow {
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  font-weight: 510;
  color: var(--accent);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  margin-bottom: var(--space-4);
}
.hero h1 {
  font-family: var(--font-display);
  font-size: var(--text-4xl);
  font-weight: 700;
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-display);
  color: var(--fg);
}
.hero h1 span {
  color: var(--accent);
}
.hero .lead {
  font-size: var(--text-xl);
  color: var(--fg-2);
  margin-top: var(--space-4);
  max-width: 52ch;
}
.hero-actions {
  margin-top: var(--space-8);
  display: flex;
  gap: var(--space-3);
  flex-wrap: wrap;
}
.hero-visual {
  display: flex;
  align-items: center;
  justify-content: center;
}
.placeholder-avatar {
  width: 240px;
  height: 240px;
  border-radius: var(--radius-lg);
  background: var(--surface-warm);
  border: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  color: var(--muted);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  transition: box-shadow 0.6s var(--ease-standard);
}
.placeholder-avatar.glow {
  animation: avatarGlow 3s ease-in-out infinite;
}

@media (max-width: 768px) {
  .hero-grid { grid-template-columns: 1fr; gap: var(--space-8); }
  .hero-visual { order: -1; }
  .hero h1 { font-size: var(--text-2xl); }
  .hero .lead { font-size: var(--text-base); }
  .placeholder-avatar { width: 160px; height: 160px; }
}

@media (max-width: 480px) {
  .hero h1 { font-size: var(--text-2xl); }
}
</style>
