<section class="contact" id="contact">
  <div class="container contact-grid">
    <div>
      <p class="section-label reveal" data-reveal-delay="0" data-i18n="contact.label">Contacto</p>
      <h2 class="section-title reveal" data-reveal-delay="80" data-i18n="contact.title">Trabajemos juntos</h2>
      <p class="reveal contact-intro" data-reveal-delay="120" data-i18n="contact.intro">Disponible para proyectos freelance, consultoría técnica y colaboraciones en etapas tempranas.</p>
      <div class="reveal" data-reveal-delay="180" style="margin-top: var(--space-4);">
        <span class="availability-badge"><span class="dot"></span> <span data-i18n="contact.badge">Disponible para nuevos proyectos</span></span>
      </div>
    </div>
    <div class="contact-links">
      <a href="mailto:potter.laynar@gmail.com" class="contact-link reveal" data-reveal-delay="0">
        <span class="link-icon"><img src="https://cdn.jsdelivr.net/npm/lucide-static@latest/icons/mail.svg" alt="" loading="lazy"></span> potter.laynar@gmail.com
      </a>
      <a href="mailto:roberto.robles@nopalsoft.net" class="contact-link reveal" data-reveal-delay="80">
        <span class="link-icon"><img src="https://cdn.jsdelivr.net/npm/lucide-static@latest/icons/mail.svg" alt="" loading="lazy"></span> roberto.robles@nopalsoft.net
      </a>
      <a href="https://github.com/RobertoLayna" class="contact-link reveal" target="_blank" rel="noopener" data-reveal-delay="160">
        <span class="link-icon"><img src="https://cdn.jsdelivr.net/npm/simple-icons@14/icons/github.svg" alt="" loading="lazy"></span> GitHub
      </a>
      <a href="https://www.linkedin.com/in/roberto-robles-layna/" class="contact-link reveal" target="_blank" rel="noopener" data-reveal-delay="240">
        <span class="link-icon"><svg viewBox="0 0 24 24" fill="#0A66C2" xmlns="http://www.w3.org/2000/svg"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg></span> LinkedIn
      </a>
      <a href="https://wa.me/5219981714200" class="contact-link reveal" target="_blank" rel="noopener" data-reveal-delay="320">
        <span class="link-icon"><img src="https://cdn.jsdelivr.net/npm/simple-icons@14/icons/whatsapp.svg" alt="" loading="lazy"></span> WhatsApp
      </a>
    </div>
  </div>
</section>

<style>
.contact {
  padding: var(--section-y-desktop) 0;
  border-top: 1px solid var(--border-soft);
}
.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-12);
  align-items: start;
}
.contact-intro {
  color: var(--fg-2);
  margin-top: var(--space-3);
  max-width: 48ch;
}
.contact-links {
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
  margin-top: var(--space-6);
}
.contact-link {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  font-family: var(--font-mono);
  font-size: var(--text-sm);
  font-weight: 510;
  color: var(--fg-2);
  text-decoration: none;
  letter-spacing: 0.02em;
  padding: var(--space-3) var(--space-4);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  transition: transform 0.2s var(--ease-standard), border-color var(--motion-fast) var(--ease-standard), color var(--motion-fast) var(--ease-standard), box-shadow 0.2s var(--ease-standard);
}
.contact-link::after {
  content: "\2197";
  margin-left: auto;
  font-size: var(--text-sm);
  opacity: 0;
  transform: translate(-4px, 4px);
  transition: opacity 0.2s var(--ease-standard), transform 0.2s var(--ease-standard);
}
.contact-link:hover::after {
  opacity: 1;
  transform: translate(0, 0);
}
.contact-link:hover {
  border-color: var(--accent);
  color: var(--fg);
  transform: translateX(4px);
  box-shadow: 4px 0 0 0 color-mix(in oklab, var(--accent), transparent 80%);
}
.contact-link .link-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 20px;
  height: 20px;
  flex-shrink: 0;
  color: var(--fg-2);
}
.contact-link .link-icon svg,
.contact-link .link-icon img {
  width: 100%;
  height: 100%;
  display: block;
}

.availability-badge {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  font-weight: 510;
  color: var(--fg-2);
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-pill);
  padding: var(--space-2) var(--space-4);
  letter-spacing: 0.02em;
}
.availability-badge .dot {
  width: 6px;
  height: 6px;
  background: var(--success);
  border-radius: 50%;
  animation: dotPulse 2s ease-in-out infinite;
}

@media (max-width: 768px) {
  .contact { padding: var(--section-y-tablet) 0; }
  .contact-grid { grid-template-columns: 1fr; gap: var(--space-8); }
}
@media (max-width: 480px) {
  .contact { padding: var(--section-y-phone) 0; }
}
</style>
